- if todo_item.done?
  %p{style: "opacity: 0.7;"}
    %strike= todo_item.title
  = link_to "Done", done_todo_list_todo_item_path(@todo_list, todo_item.id), method: :put
- else
  %p= todo_item.title
  = link_to "Done", done_todo_list_todo_item_path(@todo_list, todo_item.id), method: :put
